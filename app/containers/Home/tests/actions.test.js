import { defaultAction } from '../home.actions';
import { DEFAULT_ACTION } from '../home.constants';

describe('Home actions', () => {
  describe('Default Action', () => {
    it('has a type of DEFAULT_ACTION', () => {
      const expected = {
        type: DEFAULT_ACTION,
      };
      expect(defaultAction()).toEqual(expected);
    });
  });
});
